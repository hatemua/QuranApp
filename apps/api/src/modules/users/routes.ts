import type { FastifyInstance } from 'fastify';

export async function registerUsersRoutes(_app: FastifyInstance): Promise<void> {
  // POST /users/me/devices, /users/me/saved-words are implemented in prompt 4.
}
