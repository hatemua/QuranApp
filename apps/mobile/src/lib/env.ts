// Backend API base URL. No trailing slash.
const API_URL = 'https://agents.api.talentailabs.com:3005';

// Set to true to use canned data without talking to the backend.
const USE_MOCKS = false;

export const env = {
  API_URL,
  USE_MOCKS,
};
